# Frontend environment variables for Vite.
# NOTE: everything here is PUBLIC (baked into the client bundle). Never put the
# Telegram bot token or any secret in a VITE_* variable.
VITE_API_BASE_URL=http://localhost:8000/api/v1
